/*
 *
 * Package: `{{{ PROJECT_NAME_SPACE }}}/{{ properName }}`.
 *
 */
{{#if (isPage type)}}
import { lazy } from "react";

export default lazy(() => import("./component" /* webpackChunkName: "{{{ removeAt PROJECT_NAME_SPACE }}}.{{ properName }}" */)
);
 {{else}}
import createLazyLoadedComponent from "{{{ PROJECT_NAME_SPACE }}}/react-lazy";

export default createLazyLoadedComponent(() => import("./component" /* webpackChunkName: "{{{ removeAt PROJECT_NAME_SPACE }}}.{{ properName }}" */));
{{/if}}
