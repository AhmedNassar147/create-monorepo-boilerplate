/*
 *
 * Package:  `@domain/pkg1`.
 *
 */
const ReactComponent = ({ name }: { name: string }): JSX.Element => (
  <div>
    {name}
    {name}
    <div>@domain/pkg1</div>
  </div>
);

export default ReactComponent;
